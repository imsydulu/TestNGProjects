package com.listenerz;

import org.testng.IRetryAnalyzer;
import org.testng.ITestResult;

public class RetryAnalyzer {
	/*
	 * private int count = 0;
	 * 
	 * @Override public boolean retry(ITestResult result) {
	 * 
	 * 
	 * System.out.println("result.isSuccess()=> " + result.isSuccess()); if (count <
	 * 2) { System.out.println("retry======>>>>"); count++; return true; }
	 * 
	 * return false; }
	 */

}
